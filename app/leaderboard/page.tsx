'use client';
import { useEffect, useState } from 'react';
import { Building2, Radio, Trophy } from 'lucide-react';
const fallback=[{name:'Rahul Sharma',company:'ABC Bank',totalPoints:185,sessionsAttended:8,engagementScore:105},{name:'Amit Jain',company:'XYZ Finance',totalPoints:180,sessionsAttended:8,engagementScore:100},{name:'Neha Gupta',company:'PQR Insurance',totalPoints:175,sessionsAttended:8,engagementScore:95},{name:'Rohan Mehta',company:'ABC Bank',totalPoints:170,sessionsAttended:7,engagementScore:100},{name:'Karan Singh',company:'XYZ Bank',totalPoints:165,sessionsAttended:7,engagementScore:95}];
export default function Leaderboard(){const [rows,setRows]=useState(fallback);useEffect(()=>{const load=()=>fetch('/api/leaderboard').then(r=>r.json()).then(d=>d.leaderboard?.length&&setRows(d.leaderboard)).catch(()=>{});load();const t=setInterval(load,5000);return()=>clearInterval(t)},[]);return <main className="event-grid min-h-screen bg-[#07131f] text-white"><header className="mx-auto flex h-20 max-w-6xl items-center justify-between px-5"><a href="/" className="flex items-center gap-3"><Building2 className="size-5 text-cyan-300"/><strong className="tracking-[.12em]">BFSI 2030</strong></a><span className="flex items-center gap-2 text-xs text-emerald-300"><Radio className="size-3.5"/> LIVE</span></header><section className="mx-auto max-w-4xl px-5 py-10"><div className="text-center"><p className="text-xs font-semibold uppercase tracking-[.25em] text-cyan-300">Connect · Participate · Lead</p><h1 className="mt-4 text-4xl font-semibold tracking-tight sm:text-6xl">Live leaderboard</h1><p className="mt-3 text-slate-400">Rankings refresh automatically as points are earned.</p></div><div className="mt-10 space-y-3">{rows.map((r:any,i)=><div key={r.name} className={`grid grid-cols-[48px_1fr_auto] items-center gap-3 rounded-2xl border p-4 sm:grid-cols-[56px_1fr_120px_110px] ${i===0?'border-amber-300/30 bg-amber-300/[.07]':'border-white/[.08] bg-white/[.035]'}`}><span className={`grid size-10 place-items-center rounded-xl font-bold ${i===0?'bg-amber-300 text-[#211900]':'bg-white/[.06] text-slate-300'}`}>{i===0?<Trophy className="size-5"/>:i+1}</span><div><p className="font-semibold sm:text-lg">{r.name}</p><p className="text-xs text-slate-500">{r.company}</p></div><div className="hidden text-center sm:block"><strong>{r.sessionsAttended}</strong><span className="block text-[11px] text-slate-500">SESSIONS</span></div><strong className="text-right text-xl text-cyan-300 sm:text-2xl">{r.totalPoints}<span className="block text-[10px] font-medium text-slate-500">POINTS</span></strong></div>)}</div><p className="mt-8 text-center text-xs text-slate-600">Participant mobile numbers are never shown publicly.</p></section></main>}
