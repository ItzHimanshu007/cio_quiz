import { audit, db, hashCode, json, LIVE_SESSION_ID, now } from '@/lib/event-server';
import { getChatGPTUser } from '@/app/chatgpt-auth';
export async function POST(request:Request) { const user=await getChatGPTUser(); if(!user) return json({error:'Sign in required.'},401); const body=await request.json().catch(()=>({})); const sessionId=typeof body.sessionId==='string'?body.sessionId:LIVE_SESSION_ID; const random=new Uint32Array(1); crypto.getRandomValues(random); const code=String(1000+(random[0]%9000)); const stamp=now(); const expiresAt=stamp+60; const hash=await hashCode(sessionId,code); await db().batch([db().prepare(`UPDATE session_codes SET revoked_at=? WHERE session_id=? AND revoked_at IS NULL`).bind(stamp,sessionId),db().prepare(`INSERT INTO session_codes (id,session_id,code_hash,display_code,expires_at,created_at,revoked_at) VALUES (?,?,?,?,?,?,NULL)`).bind(crypto.randomUUID(),sessionId,hash,code,expiresAt,stamp)]); await audit(user.userId,'GENERATE_CODE','session',sessionId,{expiresAt}); return json({code,expiresAt,serverTime:stamp}); }
