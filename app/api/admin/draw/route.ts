import { audit, cleanText, db, json, now } from '@/lib/event-server';
import { getChatGPTUser } from '@/app/chatgpt-auth';
export async function POST(request:Request){const user=await getChatGPTUser();if(!user)return json({error:'Sign in required.'},401);const body=await request.json().catch(()=>({}));const existing=await db().prepare(`SELECT id FROM draws WHERE status='COMPLETED' AND prize=? ORDER BY created_at DESC LIMIT 1`).bind(cleanText(body.prize)||'Highest Session Attendee Gift').first();if(existing)return json({error:'A completed draw already exists for this prize.'},409);const top=await db().prepare(`SELECT max(total_points) as value FROM scores`).first<{value:number}>();if(!top?.value)return json({error:'No scored participants are eligible yet.'},409);const finalists=await db().prepare(`SELECT p.id,p.full_name as name,p.company,s.total_points as points FROM scores s JOIN participants p ON p.id=s.participant_id WHERE s.total_points=? AND p.enabled=1 ORDER BY p.id`).bind(top.value).all();if(finalists.results.length<2)return json({error:'A lucky draw is available only when the highest score is tied.'},409);const random=new Uint32Array(1);crypto.getRandomValues(random);const winner=finalists.results[random[0]%finalists.results.length] as any;const id=crypto.randomUUID();const stamp=now();const prize=cleanText(body.prize)||'Highest Session Attendee Gift';const statements=[db().prepare(`INSERT INTO draws (id,prize,winner_participant_id,eligible_json,status,created_at,completed_at) VALUES (?,?,?,?, 'COMPLETED',?,?)`).bind(id,prize,winner.id,JSON.stringify(finalists.results.map((f:any)=>f.id)),stamp,stamp),...finalists.results.map((f:any)=>db().prepare(`INSERT INTO draw_participants (draw_id,participant_id) VALUES (?,?)`).bind(id,f.id))];await db().batch(statements);await audit(user.userId,'COMPLETE_DRAW','draw',id,{prize,finalists:finalists.results.length,winnerId:winner.id});return json({draw:{id,prize,finalists:finalists.results,winner}});}
